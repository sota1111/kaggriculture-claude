# SOT-2368 — 公開frontierノートブックのtape蒸留championを移植

## 出典 / provenance

- 参考: 公開Kaggleノートブック
  [prvsiyan/kaggriculture-frontier-the-moon-counts-melons](https://www.kaggle.com/code/prvsiyan/kaggriculture-frontier-the-moon-counts-melons)
  ("The Moon Counts Melons")。人間指示による移植。
- 実体: 上位プレイヤー **Tran H Hoang の公開 submission 55203154, episode 89674601, seat 0** を蒸留した
  720ターン固定アクションテープ (`TRACE_ACTIONS`) + 復旧ロジック(WEED時DIG→pasture再試行等) +
  **step 309 の公開状態ゲート付き修正**(相手が wheat≥3 / strawberry≥32 / melon≤6 / cow≥7 / sheep≥5 かつ
  townが BAKERY+BRUNCH_SPOT+SMOOTHIE_SHOP 解放時のみ hand2 `PLANT WHEAT`→`PASS`)。
- `main.py` はノートブックの frozen source と **byte-identical**:
  sha256 `7c5a4a2a3ba51cc4dc00657614f28a0afa8ee275ce6cdd8e7cb6c2ac2b54840d`(ノートブック内の
  `EXPECTED_SHA256` と一致することを抽出時に検証済み)。
- ノートブック側の検証実績(公開値): historical traces 283/288 · diverse opponents 166/168 ·
  post-freeze holdout 48/48 · newest top eight 113/128。

## ローカルA/B (公式シミュ, 両席)

対戦相手 = 旧champion (SOT-2342 fertilizer-synergy, COW3/SHEEP3 + glut-aware crops)。

- **screen** seeds 101,202 ×両席: 4/4 WIN。
- **confirm** seeds 7,42,303,404,505,777,1234,2026,5555,9001 ×両席: **20/20 ALL WIN**,
  diff_mean **+112,345**, diff_min **+102,102**。cand ~153k–185k vs champ ~45k–62k。
- **self-mirror** seeds 11,222 ×両席: 122k–144k/player で拮抗(margin ±606以内)、共倒れなし。
- 全ゲーム status `['DONE','DONE']` 完走。

旧championの累計レバー(+9k級)に対し2桁上のジャンプ。トップメタ(大規模strawberry+cow/sheep経済+
melon+hire多重+demand shop解放)を丸ごと取り込む形。

## ゲート

- `scripts/validate_submission.py` PASS(契約: agent(obs)→{farmer,hands,market})。
- kaggle-exec-runtime-gate PASS: tarball展開 `main.py` を `exec`(no `__file__`, cwd=/)で実行し
  step0 契約確認。import は `copy` のみ。
- `submission.tar.gz` 再構築(root `main.py` 1ファイル)。

## 判定

**promoted** — main.py を tape champion に差替。旧 fertilizer-synergy champion は git 履歴
(eef4ce2) に保持。次の改善軸はノートブック結論と同じく「残る frontier 敗北(Superallen 等)への
公開状態ゲート付き修正の追加凍結」。
