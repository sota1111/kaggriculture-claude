# SOT-2379 — Kaggle順位向上サイクル第2次 親集約・提出判定

**Result:** New artifact submitted (soil `00e8ab59`) → **real-LB regression (public 600.0)** → champion
reverted to the LB-proven moon-198 tape (`7c5a4a2a`, public 2543.4). Oracle drift **empirically
confirmed**.

## Children (all Done)

| Child | Axis | Local result | Champion effect |
|---|---|---|---|
| SOT-2381 | External-knowledge port: Soil Remembers Rain frontier (`00e8ab59`, 332 lines) | **promoted** — 20/20 ALL WIN vs moon-198, diff_mean +170519 | set champion = soil |
| SOT-2382 | Arch: seat-diverse dual-route robustness | **rejected** — champion seat-invariant; v16 source deleted/retired | main.py unchanged |
| SOT-2383 | Data/oracle: field-representative replay-league re-anchoring | **inconclusive** — soil only 58.3% field; moon-449 field-superior (81.7%) but did not clear 20/20 confirm bar | main.py unchanged |

## Submission performed (parent-only, per issue contract)

Submitted the child-promoted soil champion via the control-plane gate:

```
bash scripts/ai/kaggle_targets_submit.sh --competition kaggriculture --repo kaggriculture-claude --execute
```

- Kaggle **ref 55220390**, file `submission.tar.gz`, artifact sha256 `d7cd8867…`,
  `SubmissionStatus.COMPLETE`, **public = 600.0**.
- Pre-submit gates PASS: `validate_submission` contract, exec-compat (no `__file__`, cwd `/`, `agent()`
  present), tarball main.py == `00e8ab59`.
- Artifact fingerprint `d7cd8867` ≠ prior `e7dfd915` → a **genuine new submission** (not a
  fingerprint-skip), satisfying acceptance criterion 4.

## Divergence: local oracle vs real LB (oracle drift, confirmed)

| Artifact | main.py sha | Local self-mirror A/B | Real LB public | Kaggle ref |
|---|---|---|---|---|
| moon-198 (SOT-2368) | `7c5a4a2a` | baseline | **2543.4** | 55211714 (08-03 12:34) |
| soil (SOT-2381) | `00e8ab59` | 20/20 ALL WIN vs moon-198, +170519 | **600.0** (noise floor) | 55220390 (08-03 19:47) |

The self-mirror + vs-own-champion oracle rated soil overwhelmingly dominant, yet the real arena floored
it (600.0 = the `<600` relative-scoring noise band). This is a **4× regression** and a hard confirmation
of the oracle-drift SOT-2383 hypothesized. It is independently corroborated by SOT-2383's
field-representative replay league (soil 58.3% field; updated moon-449 frontier 81.7% field, handling
kaitofukami_v17 88% vs soil's 46%). **Two independent field oracles agree soil is field-inferior.**

## Decision: revert champion-of-record to moon-198

- Reverted `main.py` to the LB-proven moon-198 tape (`7c5a4a2a`, 198 lines) — the artifact that actually
  scored **2543.4** on the real LB. Rebuilt `submission.tar.gz` (contract PASS, exec-compat PASS, tarball
  main.py `7c5a4a2a`).
- Rationale: the primary KPI is real-LB rank; local A/B is a proxy that is now proven drifted for this
  lineage. Keeping the LB-inferior soil champion would knowingly degrade the base for future cycles. This
  revert is to an already-proven, byte-identical artifact — no new code risk.
- The daily submission cap (2/lineage) is **exhausted** today (12:34 moon 2543.4 + 19:47 soil 600.0), and
  Kaggle ranks on your **best** submission, so moon-198's 2543.4 remains the standing LB entry — **rank
  is unaffected** by the soil regression. No re-submission is needed or possible today.

## Next-cycle guidance (cycle 3)

- **Do NOT trust self-mirror / vs-own-champion A/B alone for this lineage** — drift is now empirically
  proven. Gate promotions on the field-representative replay league (`scripts/eval_league.py`, SOT-2383)
  and/or real-LB confirmation before committing a champion.
- **moon-449** (updated frontier, SOT-2383 replay-league field-superior 81.7%) is the leading improvement
  candidate — but it needs its own real-LB confirmation before promotion; the last such promotion (soil)
  looked even stronger locally and floored on the real LB.
